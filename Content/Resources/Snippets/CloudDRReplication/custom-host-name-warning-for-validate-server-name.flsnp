<?xml version="1.0" encoding="utf-8"?>
<html xmlns:MadCap="http://www.madcapsoftware.com/Schemas/MadCap.xsd">
  <body>
    <p class="Caution">If you are using custom certificate hostnames,  run <code>no validate-server-name</code> instead of <code>validate-server-name</code> in the steps below, otherwise the replication won't function (for example, the replication bridge will show a  <code>down</code> state and the replication queue can't be bound).</p>
  </body>
</html>
